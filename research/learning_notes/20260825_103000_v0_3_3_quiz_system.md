# 学习笔记: v0.3.3_quiz_system

- **时间**: 2026-08-25T10:30:00
- **版本**: 0.3.3
- **依据文档**: Cyborg_Trading_System_Summary.md (人机协作自营交易系统·第一阶段: 理论定量化与试卷盲测系统)

## 决策路径 (Decision Path)
1. 需求: 将 Cyborg 试卷盲测框架落地为网页版答题交互系统, 理论框架全部对齐 Summary 文档
2. 设计: 随机截取盲盒(120×4H默认, 隐藏未来) → 三模块答题(强弱度/努力结果/盈亏比) → 自动批改(未来60根) → MFE/MAE标注 → 错题分析 → 黄金标注集导出
3. 批改数学: 止损优先 (与回测引擎一致); 出场价=止损价, 跳空按开盘价成交; TARGET=触及止损前达2R→100分, STOP=破止损→0分, TIMEOUT=60根耗尽→50分
4. MFE/MAE: 以R倍数记录, **含出场当根K线极值** (标准定义); MAE同时换算为占止损距离百分比
5. 观望/放弃路径: 混沌(C)或盈亏比不足(B)不交易, 但用建议止损模拟"如果入场"的潜在MFE, 检验克制是否正确 (双向均<2R = 正确观望)
6. 数据保密: 盲盒载荷只含可见K线, 未来数据仅存服务端 (`future_bars`), 批改后才下发 — 杜绝作弊
7. 防重复: 同一 item_id 只能批改一次; 会话内已用起点去重

## 结果快照 (Results Snapshot)

- **冒烟测试** (scripts/quiz_smoke.py): 做多/做空/观望/盈亏比放弃全路径通过; 批改数学断言: TARGET→MFE≥2R, STOP→MAE≥1R; 未来数据零泄露
- **API 闭环验证**: new(120可见+未来隐藏) → grade(三种结局+60根揭晓) → stats → export → download 全通; 重复交卷 HTTP 400 拦截
- **标注格式**: `[K线矩阵(120,5)] + [human: m1/m2/m3/方向/进场/止损] + [outcome: 结局/分数/MFE/MAE/出场]` → research/quiz/labels/quiz_labels_*.jsonl

## 修复记录 (Bugs Found & Fixed)

| Bug | 根因 | 修复 |
|---|---|---|
| 批改报错 'future' | quiz_new 的 item.pop("future") 删除了 self.items 中同一引用的未来数据 | new_item 返回浅拷贝, 服务端保留完整 item |
| 空头 MFE/MAE 反向 | 空头有利=下跌, 误用最高价计算 MFE | 空头 MFE 用最低价, MAE 用最高价 |
| MFE/MAE 漏掉出场当根 | 极值更新在出场判定之后 | 先更新极值再判定出场 |
| 标注文件含整段未来K线 | 记录里把 future 条数误存为 bars 列表 | 拆分 `future`(int) / `future_bars`(服务端专用) |

## 优化方向 (Optimization Direction)

- 用积累的标注集直接训练 CNN/LSTM (Summary 第三章): features → human label → outcome
- 错题规则自动迭代 (Summary 第二章): 按模块2信号分组统计"画饼率"回写判断逻辑
- 支持自定义止损点位测试 (当前为输入框, 可加"点击图表设止损"交互)
- OCR 课件语料接入同一答题体系 (知识点提示模式)

---
*由 KNOWLEDGE_MANAGEMENT_LAYER 自动生成*
